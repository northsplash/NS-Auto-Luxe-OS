# North Splash OS

Authenticated business/customer operating system intended for `app.northsplash.com`.

Includes Admin, Manager, D2D, Detailer/Employee and Customer portals. It uses the same Supabase project as the public website, so bookings and customer activity share one source of truth.

The Admin workspace also includes a new global command/search palette and displays guest booking contacts alongside registered customers.
